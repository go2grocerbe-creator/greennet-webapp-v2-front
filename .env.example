# Keep mock mode enabled throughout design approval.
NEXT_PUBLIC_DATA_SOURCE=mock

# Add only when the existing GreenNet API is ready to be connected.
NEXT_PUBLIC_GREENNET_API_BASE_URL=

# Preview or production origin used for generated metadata.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
